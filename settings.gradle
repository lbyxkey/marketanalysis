pluginManagement {
    repositories {
        maven { url 'https://repo.spring.io/release' }
        maven { url 'https://repo1.maven.org/maven2' }
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'marketanalysis'

